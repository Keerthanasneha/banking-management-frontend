//  configures TanStack Query's global server-state management behavior.

// TanStack Query handles server state.

import { QueryClient } from "@tanstack/react-query";

export const queryClient = new QueryClient();
